* hello.mixal: this programs echo hello world to STDOUT
* I am able to run this code directly inside of Emacs using
* mixal-mode, (require 'mixvm)
* label ins operand comment
* last commnet line
TERM  EQU 19  MIX console number
      ORIG 3000 start address
START OUT MSG(TERM)
      HLT
* ALF outputs five characters
MSG   ALF "MIXAL"
      ALF " HELL"
      ALF "O WOR"
      ALF "LD   "
      END START
